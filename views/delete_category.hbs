{{#extends 'base'}}


{{#block "content"}}
<div class="mt-2 alert alert-danger">
    Are you sure you want to delete this category: {{category.category}}?
    <div>
        <form action="/category/delete/{{this._id}}" method="POST">
            <button type="submit" class="btn btn-danger">Yes</button>
            <a href="/category" class="btn btn-success">No</a>
        </form>
    </div>
</div>
{{/block}}

{{/extends}}